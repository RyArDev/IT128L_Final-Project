import { PasswordConfirmationValidator } from './password-confirmation-validator.model';

describe('PasswordConfirmationValidator', () => {
  it('should create an instance', () => {
    expect(new PasswordConfirmationValidator()).toBeTruthy();
  });
});
